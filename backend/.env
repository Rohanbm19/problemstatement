# SQLite Database (Default for simple local execution)
DATABASE_URL=sqlite:///./app.db

# PostgreSQL Database (Uncomment and set credentials if using local PostgreSQL)
# DATABASE_URL=postgresql://postgres:2006@localhost:5432/twinstock_db